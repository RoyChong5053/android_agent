# Project Plan

## 开发方针（2026-09-19 定调）

> **痛点驱动的小脚本优先，全自动化后置。**

- 遇到一个具体重复操作痛点 → 写一个独立傻瓜脚本（模板匹配 + 定时点击），跑通够用即可。
- 小脚本成熟后再考虑整合成端到端自动流程。
- 旧的 anchor 状态机（`screens.py` + `giant_b9.py`）保留但非重点。
- 每个脚本配 `core/skills/<name>.md` 记录流程/坐标/模板/坑。

## Completed
- [x] 拉莫遗址 5 阶段刷经验傻瓜脚本（`core/skills/lamo_ruins.py` + `lamo_ruins.md`）
      实跑 2 局全自动，每局约 157s。模板 `data/templates/lamo/`。
- [x] 竞技场刷翅膀脚本（`core/skills/arena.py` + `arena.md`）
- [x] 项目结构重组（Perception/Brain/Action）
- [x] MVP「连续战斗」skill（`giant_b9.py`，旧状态机方案）
- [x] anchor 状态机 + X 按钮模板关弹窗

## In Progress
- [ ] 拉莫遗址脚本：跑更长时间稳定性验证（体力耗尽 / 自动续战中断 / 闪退恢复）

## Backlog - 近期（继续攒小脚本）
- [ ] 盘点其他痛点，按「重复次数 × 烦人程度」排序，逐个做脚本
- [ ] 脚本通用骨架：模板加载 / 优先级识别 / 定时点击 / journal 日志（从 lamo_ruins 提炼）
- [ ] 每个脚本的模板回归：重启/游戏更新后跑一遍截图用例，确认阈值 0.85 仍干净
- [ ] 前台包名校验（tap 前确认焦点是游戏，防误点切后台）
- [ ] 异常自愈：卡在未知画面时截图存证 + 重试，而非干等

## Backlog - 远期（小脚本成熟后）
- [ ] 编排器：体力耗尽自动切换脚本、定时任务、完成提醒
- [ ] OCR 读取动态文本（价格 / 次数 / 战力）
- [ ] YOLO 处理形状多变但类别固定的元素
- [ ] 本地 VL 常驻 watcher（后台每 N 秒分类截图记 journal，只做分类/读数）

## 已搁置（旧方向，保留作参考）
- [ ] 强化模板匹配体系（动态 ROI、多版本模板库）
- [ ] 增强帧差验证（颜色 + 局部区域，避免大厅动画误判）—— 现脚本改用模板优先级识别，基本不需要
