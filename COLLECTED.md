# 实测讯息收集（2026-09-16）

## 设备
- LGV35（adb id `LGV35fd728cd2`），USB 已连
- `wm size`：override 720x1440，物理 1440x2880
- 横屏游戏时 `screencap` 输出 **1440x720**
- **坐标映射已验证**：截图像素坐标 == `adb shell input tap` 坐标，可直接用
- scrcpy server 4.1 运行中

## 本地模型（llama.cpp :12888，可选，默认不进主循环）
- 模型：Gemma4-26B-A4B-QAT Q4_K_M，multimodal
- 接口：OpenAI 兼容 `/v1/chat/completions`
- 图片预处理：最长边 768 + JPEG70（b64 约 87KB）
- 实测延迟：分类 ~13s，grounding ~17s → 只适合每局 0-2 次低频调用
- prompt 必须加“直接回答不要推理过程”+ max_tokens=1024 + temperature 0.1，
  否则推理占满 tokens 导致 content 为空
- grounding 只能粗定位（偏差 ~60px）；固定 UI 用模板/Hough 更准更快

## 关键坑
- **勿把原图喂 LLM**：1440x720 PNG 单张 0.5~2.2MB，连读十几张触发 API 413
  并引发上下文压缩。统一用 `Frame.small_jpeg()`（~50KB）。
- **帧差验证不可靠**：大厅有动画，`changed()` 恒真 → 假成功。关键转移用 `detect_state` 复核。
- **Hough 找 X 按钮会误报**：正常画面也检测到圆。改用 X 按钮模板匹配 + 阈值 0.85。
- **模板要裁“固定文本”**：按钮上的价格/次数会变，别把数字裁进模板。
- **检测顺序=优先级**：结果页也含“进行次数”，`cont_result` 必须排在 `cont_running` 前。

## 内置「连续战斗」（MVP 核心）
- 队伍选择(连续战斗版) 按钮为 `8 ×10 连续战斗`，一次开 **10 局**，自动领奖
- 运行中画面：`进行次数 x/10` + 小窗预览 + `结束连续战斗`
- 结束画面：`连续战斗结果 10/10次` + `选择地区` / `再来一次`
- 一轮 ×10 实测约 13~14 分钟；期间可挂机关屏（PC 不必参与）

## 关键坐标（1440x720 实测）
| 元素 | 坐标 |
|---|---|
| 大厅·战斗 | (780, 655) |
| 冒险地图·卡伊洛斯地下城 | (720, 670) |
| 卡伊洛斯·巨人B9 战斗 | (1180, 250) |
| 队伍选择·连续战斗 | (1222, 565) |
| 连续战斗结果·再来一次 | (760, 616) |
| 连续战斗结果·选择地区 | (250, 616) |
| 连续战斗菜单·卡伊洛斯 | (1080, 360) |
| 符文弹窗·X（单局兜底） | (968, 196) |

## 代码（当前路径）
- `core/perception/capture.py` Frame 采集/解码一次/小图 · `core/brain/state.py` 帧差
- `core/action/adb_wrapper.py` tap/swipe/换算 · `core/perception/detectors.py` 模板/颜色/Hough 原语
- `core/perception/screens.py` detect_state + 模板定义 · `core/perception/popups.py` 通用关弹窗
- `core/utils/logger.py` Journal JSONL 日志 · `core/perception/probe.py` 受控探查
- `main_runner.py` CLI · `core/skills/giant_b9.py` 连续战斗刷本
- 旧原型：`loop.py` / `wait_static.py`（已弃用）

---

## 项目分析与开发方向建议 (2026-09-17)

### 现状评估
项目架构完整（Perception → Brain → Action），MVP「连续战斗刷本」已通过模板匹配跑通。核心痛点在于：
1. **模板脆弱性**：UI微调、分辨率变化、动态文本导致失效
2. **叠加层干扰**：弹窗覆盖结算页，检测顺序必须严格分层
3. **帧差验证不可靠**：大厅动画导致 `changed()` 恒真产生假成功

### 推荐开发方向

#### Phase 1: 强化模板匹配体系（当前重点）
- 动态ROI提取：从模板中剥离会变动的数字/文本，只保留固定背景
- 多版本模板库：为不同游戏版本维护template文件夹，启动时按版本号加载
- 增强帧差验证：结合颜色变化+局部区域对比，避免全局动画误判

#### Phase 2: 引入轻量OCR
- 对位置不固定但文字模式固定的元素（如"进行次数 x/10"）使用PaddleOCR或ADB textcontrol
- OCR结果与坐标定位结合验证

#### Phase 3: YOLO数据管线（如需处理形状多变元素）
- 批量截图+状态打标签收集脚本
- labelImg/roboflow标注流程
- YOLOv8n模型训练与集成

> 注：以上 Phase 1-3 为旧方向，2026-09-19 起改为「痛点驱动小脚本优先」，详见 README/PLAN。

---

## 拉莫遗址(次元·哈勒普 5阶段)刷经验实测（2026-09-19）

**痛点**：该次元地下城每局结束都要手动点「再来一次」刷经验，极烦。
**方案**：`core/skills/lamo_ruins.py`（傻瓜脚本，模板匹配 + 定时点击）。模板 `data/templates/lamo/`。
**结果**：实跑 2 局全自动，每局约 157s。日志 `data/journal/run_*.jsonl`。

### 单局流程
```
队伍选择 --开始战斗--> [弹窗：是(领袖技能)/是(探索战斗)] --加载~12s-->
自动战斗(x3, ~150s) --胜利--> 奖励结算(奖励/经验) --点屏幕推进开箱-->
[确认(掉落弹窗)] --> 再来一次 --点击--> 下一局
```

### 关键实测
- **游戏内置「连续战斗」会自动续下一局**：第一局点「开始战斗」后，胜利→再来一次后自动进第二局，无需再点。所以脚本主要处理「胜利后的结算段」。
- 自动续战会停（约 3 局或出符文时），停下后回到「拉莫遗址详情弹窗(战斗开始)」或「队伍选择(开始战斗)」，脚本已兜底这两步。
- 战斗画面所有模板相似度最高仅 ~0.55，阈值 0.85 不会误触。
- 一次性 popup（领袖技能/探索战斗）约 24s 无操作会**自动消失**；脚本也会主动点「是」。
- 奖励结算画面（胜利+奖励+经验）**无按钮**，需点屏幕推进：先点 (720,500) 开箱，再点 (720,370) 进下一步。

### 关键坐标（1440x720，实测）
| 元素 | 坐标 |
|---|---|
| 队伍选择·开始战斗 | (1193, 497) |
| 弹窗·是（领袖技能） | (607, 434) |
| 弹窗·是（探索战斗） | (605, 476) |
| 奖励结算·推进开箱 | (720, 500) → (720, 370) |
| 掉落弹窗·确认 | (720, 602) |
| 胜利·再来一次 | (490, 389) |
| 胜利·停止 | (950, 389) |
| 遗址详情弹窗·战斗开始 | (1029, 585) |

### 模板（`data/templates/lamo/`，阈值 0.85）
`popup_yes`(是，领袖/探索通用) · `confirm`(确认) · `again`(再来一次) · `battle_start`(战斗开始) · `start_battle`(开始战斗) · `reward_title`(奖励)

### 坑
- **开始战斗模板会隔着弹窗命中**（队伍选择在弹窗后面仍可见），所以 `popup_yes` 必须排在 `start_battle` 前面检测。
- 两个弹窗的「是」按钮几乎一样，一个 `popup_yes` 模板即可，按匹配位置点击。
- 结算时「确认」会连续命中两次（(720,598) 和 (720,553)，两个掉落弹窗），照点即可。
